'use strict';
let hasDrivingLicense=true;
const passTest=true;

if (passTest)hasDrivingicense=true // 'L' IS MISSING//
if (hasDrivingLicense)
	console.log('you can drive')

// IF WE NOT USE STRICT MODE IT WILL GIVE OUT PUT//